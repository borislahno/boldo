import {css} from '@emotion/react';

export const styles = {
	blog: css({
		padding: "0 20px 124px 20px",
	}),
	subtitle: css({
		marginBottom: "12px",
	}),
	heading: css({
		maxWidth: "842px",
		margin: "0 auto 77px auto",
	}),
	postContainer: css({
		display: "flex",
		justifyContent: "center",
		gap: "50px",
		marginBottom: "84px",
	}),
	button: css({
		width: "219px",
		height: "60px",
		margin: "0 auto",
	}),
}