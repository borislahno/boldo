import {css} from '@emotion/react';
import {colors} from "../../styles/colors";

export const styles = {
	testimonials: css({
		padding: '96px 20px',
		backgroundColor: colors.darkBlue,
	}),

	heading: css({
		maxWidth:"716px",
		color: colors.white,
		textAlign: "left",
	}),
}